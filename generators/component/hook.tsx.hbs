import { useEffect } from 'react';

export interface {{pascalCase name}}I {
}

export const {{camelCase name}} = ({}: {{pascalCase name}}I) => {
  useEffect(() => {

    return () => {

    };
  }, []);
};
