import React from 'react';
import {{pascalCase name}} from './';
import { Meta, ComponentStory } from '@storybook/react';

export default {
  title: 'Organims/{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta;

const Story = (args: any) => <{{pascalCase name}} {...args} />;

export const Default: ComponentStory<typeof {{pascalCase name}}> = Story.bind({});

const Props = {};

Default.args = Props;
